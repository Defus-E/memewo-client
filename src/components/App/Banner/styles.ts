import styled from 'styled-components';

export const StyledBanner = styled.div`
  margin: 40px 0;

  img {
    margin: 0 20px;
    width: calc(100% - 40px);
    height: 300px;
    object-position: center;
    object-fit: cover;
  }
`;
